---
title: 7-华为技术一面—有效的括号
date: 2025-12-26 13:45:30
excerpt: 利用栈结构实现括号的匹配校验，确保每种类型的左括号都能按正确顺序与右括号闭合。
tags:
  - 面试
  - 技术一面
  - 华为
  - 栈
  - 字符串
  - 数据结构
ODtags:
  - 频率中
rating: ⭐
status: complete
destination: 03-Projects/11-笔试/02-技术一面
obsidianUIMode: preview
categories: 华为机考
---

## 一、 题目描述
给定一个只包括 `'('`，`')'`，`'{'`，`'}'`，`'['`，`']'` 的字符串 `s` ，判断字符串是否有效。

有效字符串需满足：
1. 左括号必须用相同类型的右括号闭合。
2. 左括号必须以正确的顺序闭合。
3. 每个右括号都有一个对应的相同类型的左括号。

## 二、输入描述
一个只包含字符 `'('`、`')'`、`'{'`、`'}'`、`'['`、`']'` 的字符串 `s`。

## 三、输出描述
布尔值：如果字符串有效返回 `true`，否则返回 `false`。

## 四、用例
### 用例1
**输入：** `s = "()"`  
**输出：** `true`

### 用例2
**输入：** `s = "()[]{}"`  
**输出：** `true`

### 用例3
**输入：** `s = "(]"`  
**输出：** `false`

### 用例4
**输入：** `s = "([)]"`  
**输出：** `false`

## 五、备注
*   1 <= s.length <= 10^4
*   s 仅由括号 '()[]{}' 组成

## 六、题目解析

### 1. 题目分析
本题是数据结构中“栈”的应用经典案例。括号匹配的核心逻辑是：**后出现的左括号必须先被匹配**。这种“后进先出”的特性完美对应了栈（Stack）的数据结构。

### 2. 解题思路
1.  **初始化栈**：创建一个栈用于存储左括号。
2.  **遍历字符串**：从左到右处理字符串中的每一个字符：
    *   如果遇到左括号 `(`、`[`、`{`，将其压入栈中。
    *   如果遇到右括号 `)`、`]`、`}`：
        *   首先检查栈是否为空。如果栈为空，说明没有对应的左括号，直接返回 `false`。
        *   弹出栈顶元素，检查弹出的左括号是否与当前右括号类型匹配。
        *   如果不匹配，返回 `false`。
3.  **最终检查**：遍历结束后，检查栈是否为空。
    *   如果栈为空，说明所有左括号都找到了对应的右括号，返回 `true`。
    *   如果栈不为空，说明有左括号未被闭合，返回 `false`。

### 3. 代码实现

#### Java8实现
```java
import java.util.Stack;
import java.util.HashMap;
import java.util.Map;

public class Solution {
    public boolean isValid(String s) {
        // 如果长度为奇数，肯定无效
        if (s.length() % 2 != 0) {
            return false;
        }

        // 使用Map存储匹配关系，方便查找
        Map<Character, Character> pairs = new HashMap<>();
        pairs.put(')', '(');
        pairs.put(']', '[');
        pairs.put('}', '{');

        Stack<Character> stack = new Stack<>();

        for (int i = 0; i < s.length(); i++) {
            char ch = s.charAt(i);
            // 如果是右括号
            if (pairs.containsKey(ch)) {
                // 栈为空或者栈顶元素不匹配
                if (stack.isEmpty() || !stack.peek().equals(pairs.get(ch))) {
                    return false;
                }
                // 匹配成功，弹出栈顶
                stack.pop();
            } else {
                // 如果是左括号，入栈
                stack.push(ch);
            }
        }
        
        // 最终栈为空则说明全部匹配完成
        return stack.isEmpty();
    }

    public static void main(String[] args) {
        Solution sol = new Solution();
        System.out.println(sol.isValid("()[]{}")); // true
        System.out.println(sol.isValid("([)]"));   // false
    }
}
```

### 4. 复杂度分析
*   **时间复杂度**：$O(n)$，其中 $n$ 是字符串的长度。我们只需要遍历字符串一次。
*   **空间复杂度**：$O(n)$，在最坏的情况下（例如字符串全是左括号），栈中需要存储所有的字符。

## 引用
*   华为面试经典手撕题
*   LeetCode 第 20 题

**版权声明：本文为博主原创文章，遵循 CC 4.0 BY-SA 版权协议，转载请附上原文出处链接和本声明。**
