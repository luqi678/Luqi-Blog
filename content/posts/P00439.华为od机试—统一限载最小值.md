---
title: P00439. 华为od机试—统一限载最小值
date: 2025-12-26 13:45:30
excerpt: 使用二分查找算法寻找满足干货和湿货运输要求的最小统一限载量。
tags:
  - 笔试
  - 机考
  - 华为
  - 二分查找
  - 贪心
ODtags:
  - 2025C卷
rating: ⭐⭐⭐⭐
status: complete
destination: 03-Projects/11-笔试/01-华为
obsidianUIMode: source
categories: 华为机考
---

## 一、 题目描述

火车站附近的货物中转站负责将到站货物运往仓库，小明在中转站负责调度 $2K$ 辆中转车（$K$ 辆干货中转车，$K$ 辆湿货中转车）。货物由不同供货商从各地发来，各地的货物是依次进站，然后小明按照卸货顺序依次装货到中转车上，一个供货商的货只能装到一辆车上，不能拆装，但是一辆车可以装多家供货商的货；中转车的限载货物量由小明统一制定，在完成货物中转的前提下，请问中转车的统一限载货物数最小值为多少。

## 二、输入描述

1. 第一行 `length` 表示供货商数量 $1 \le length \le 10^4$
2. 第二行 `goods` 表示供货数数组，$1 \le goods[i] \le 10^4$
3. 第三行 `types` 表示对应货物类型，`types[i]` 等于 0 或者 1（0 代表干货，1 代表湿货）
4. 第四行 `k` 表示单类中转车数量 $1 \le k \le goods.length$

## 三、输出描述

运行结果输出一个整数，表示中转车统一限载货物数。

## 四、用例

### 用例1

**输入**
```text
4
3 2 6 3
0 1 1 0
2
```
**输出**
```text
6
```
**说明**
货物1和货物4为干货，由2辆干货中转车中转、每辆车运输一个货物、限载为3。
货物2和货物3为湿货，由2辆湿货中转车中转，每辆车运输一个货物、限载为6。
这样中转车统一限载货物数可以设置为6（干货车和湿货车限载最大值），是最小的取值。

### 用例2

**输入**
```text
4
3 2 6 8
0 1 1 1
1
```
**输出**
```text
16
```
**说明**
货物1为干货，由1辆干货中转车中转，限载为3。
货物2、货物3和货物4为湿货，由1辆湿货中转车中转，限载为16。
这样中转车统一限载货物数可以设置为16（干货车和湿货车限载最大值），是最小的取值。

## 五、备注
1. 中转车最多跑一趟仓库。
2. 货物必须按进站顺序依次装载。

## 六、题目解析

### 1. 题目分析
题目要求我们找到一个**统一的最小限载量**，使得：
1. 所有的干货（类型0）能用 $K$ 辆车运走。
2. 所有的湿货（类型1）能用 $K$ 辆车运走。
3. 货物装载必须遵循原始进站顺序。即如果干货进站顺序是 $A, B, C$，那么装载时只能先装 $A$，再装 $B$，再装 $C$，不能跳跃装载。
4. 一个供货商的货不能拆分。

### 2. 解题思路
这是一个典型的“**最小化最大值**”问题，通常使用**二分查找（Binary Search）**结合**贪心算法（Greedy）**来解决。

- **二分范围**：
  - 最小可能值：数组中的最大单件货物重量（因为单件不能拆分）。
  - 最大可能值：所有货物的总重量。
- **判定函数（check）**：
  - 给定一个承载能力 `limit`，分别检查干货列表和湿货列表是否都能在 $K$ 辆车内装完。
  - 检查方法：遍历货物体积，累加到一个当前车辆，如果超过 `limit`，则开启新的一辆车，最后统计总车辆数是否 $\le K$。
- **统一限载**：
  - 因为是“统一”限载，所以一个 `limit` 必须同时满足干货和湿货的要求。

### 3. 代码实现

#### Java8实现

```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        
        // 1. 输入数据处理
        if (!sc.hasNextInt()) return;
        int n = sc.nextInt();
        
        int[] goods = new int[n];
        for (int i = 0; i < n; i++) {
            goods[i] = sc.nextInt();
        }
        
        int[] types = new int[n];
        for (int i = 0; i < n; i++) {
            types[i] = sc.nextInt();
        }
        
        int k = sc.nextInt();
        
        // 2. 将货物按类型分类，保持原始进站顺序
        List<Integer> dryGoods = new ArrayList<>();
        List<Integer> wetGoods = new ArrayList<>();
        for (int i = 0; i < n; i++) {
            if (types[i] == 0) {
                dryGoods.add(goods[i]);
            } else {
                wetGoods.add(goods[i]);
            }
        }
        
        // 3. 确定二分查找的边界
        long left = 0;
        long right = 0;
        for (int g : goods) {
            left = Math.max(left, g); // 至少要能装下最大的单件货物
            right += g;               // 最多是所有货物总和
        }
        
        long ans = right;
        
        // 4. 二分查找最小限载
        while (left <= right) {
            long mid = left + (right - left) / 2;
            if (canTransport(dryGoods, k, mid) && canTransport(wetGoods, k, mid)) {
                ans = mid;
                right = mid - 1;
            } else {
                left = mid + 1;
            }
        }
        
        System.out.println(ans);
    }
    
    /**
     * 判定在限载为 limit 的情况下，能否用 k 辆车运完 list 中的货物
     */
    private static boolean canTransport(List<Integer> list, int k, long limit) {
        if (list.isEmpty()) return true;
        
        int count = 1; // 当前使用的车辆数
        long currentLoad = 0;
        
        for (int weight : list) {
            if (currentLoad + weight <= limit) {
                currentLoad += weight;
            } else {
                count++;
                currentLoad = weight;
                if (count > k) return false;
            }
        }
        return count <= k;
    }
}
```

### 4. 复杂度分析
- **时间复杂度**：$O(N \log S)$，其中 $N$ 是供货商数量（最高 $10^4$），$S$ 是货物总重量（最高 $10^4 \times 10^4 = 10^8$）。$\log(10^8) \approx 27$，整体运算量约为 $2.7 \times 10^5$，完全符合机考时间限制。
- **空间复杂度**：$O(N)$，用于存储干货和湿货的列表。

## 引用
- 华为OD机考真题训练。
- 二分查找算法模板：应用于查找满足条件的最小值。

**版权声明：本文为博主原创文章，遵循 CC 4.0 BY-SA 版权协议，转载请附上原文出处链接和本声明。**
